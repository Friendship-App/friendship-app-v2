import React from "react";
import {Text, View} from "react-native";
import {styles} from "../../styles";

export const RegisterScreen = props => (
  <View style={[styles.rootContainer, {backgroundColor: 'red'}]}>
    <Text>Hello world 2</Text>
  </View>
);